{
  "name": "chess",
  "version": "1.0.0",
  "description": "Chess as a Service API",
  "main": "server.js",
  "scripts": {
    "dev": "npm run build && concurrently -n dotenv,tsc,server 'tsc --watch --preserveWatchOutput' 'nodemon --watch .env --watch build -e js,json --delay 2.5ms --ignore \"build/**/*.test.js\" build/server.js'",
    "build": "tsc -p tsconfig.json",
    "start": "node build/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nick Snyder",
  "license": "ISC",
  "prettier": {
    "singleQuote": true,
    "trailingComma": "es5",
    "semi": false
  },
  "devDependencies": {
    "@tsconfig/node18": "^1.0.1",
    "@types/node": "^18.11.10",
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "fastify": "^4.10.2"
  }
}
