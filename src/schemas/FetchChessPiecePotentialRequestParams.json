{
  "$id": "FetchChessPiecePotentialRequestParams.json",
  "title": "FetchChessPiecePotentialRequestParams",
  "type": "object",
  "additionalProperties": false,
  "required": ["boardId", "column", "row"],
  "description": "Contains params for requesting information about a specific chess board instance.",
  "properties": {
    "boardId": {
      "type": "string"
    },
    "column": {
      "type": "number",
      "minimum": 0
    },
    "row": {
      "type": "number",
      "minimum": 0
    }
  }
}
